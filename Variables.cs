using System;
 
namespace mtech
 
{
    class Variables {
 
       static void Main(string[] args)  {
            
            // Local Variables
            // static / class variables
            // instance variables ( Non static variables )
            // const variables

            // 5) readonly
            // same like const...But can intitalize after declaration in the constructor
            // Can't modified after initialization

            // 6) implicitly typed variables

            // var x = 10;
            // Console.WriteLine("Type of x is : " + x.GetType());

            // But main thing... it can only use as local scope variables
        }
 
    }
}